# Heightmap data — auto-generated by the Height Map Editor.
# Format: [map_id] then x,y=height lines. Height 0 (ground) tiles
# are omitted entirely since that is the default for any tile
# with no explicit entry. [map_id:flags] sections mark stairs/
# slope transition tiles separately from height.

